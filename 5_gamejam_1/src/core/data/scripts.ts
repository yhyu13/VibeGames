// core/data/scripts.ts — 剧本（TDD §5.4 冻结形状，02 §4）
// TODO agent-content: dignity + tragic 全量（3 阶段 × beats）；mad 占位（STRETCH_FLAGS.madScript）。

import type { ScriptDef } from '../types';

export const SCRIPTS: ScriptDef[] = [];

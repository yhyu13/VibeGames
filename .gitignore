.playwright-mcp/
tripo.md


# build output — regenerated by `npm run build`; never commit hashed bundle names
5_gamejam_1/dist/
# 4_chunbai/new_game/dist/ is STILL TRACKED (3 files) and will block the same --land guard.
# Untrack it before spending a round on 4_chunbai: `git rm -r --cached 4_chunbai/new_game/dist`
# then uncomment the line below.
# 4_chunbai/new_game/dist/

.playwright-mcp/
tripo.md


# build output — regenerated by `npm run build`; never commit hashed bundle names
5_gamejam_1/dist/
4_chunbai/new_game/dist/
# 4_chunbai/new_game/node_modules/ is STILL TRACKED (5801 files). It does NOT block --land
# (the gates only run `npm run <script>`, never an install, so nothing under it changes during
# a round) — it is repo poison, not a blocker. Clearing it is a separate chore.
# 4_chunbai/new_game/node_modules/

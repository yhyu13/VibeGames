{
  "name": "rc-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "2D Radiance Cascades demo — WebGL-first, WebGPU-ready (single-file port of 7_hotlineShanghai/rc-lab).",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b --noEmit",
    "preview": "vite preview",
    "check": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "three": "0.185.0"
  },
  "devDependencies": {
    "@types/three": "0.185.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}

# 渲染/图形学工程师面经 —— VibeGames 图形学项目

> 覆盖最近一个月的渲染方向改动：DDGI（12_ddgi）、ReSTIR（6_patapon3D）、Kerr 黑洞（11_blackhole）、神经纹理（14_neuraltexture）、SAR 卫星（13_spysatellite）。
> 所有答案锚定到真实代码 `文件:行号`，不写虚构出处。进场前 10 分钟过一遍第 4 节速记表即可。

---

## 0. 开场 30 秒怎么讲（结论先行）

**一句话定位：我在一个 VibeGames monorepo 里，用「平台无关纯核心 + 引擎适配器」的分层纪律，独立实现了 5 个浏览器实时渲染实验——DDGI 探针全局光照、ReSTIR 时空重采样、Kerr 黑洞测地线光线追踪、神经材质 MLP、SAR 卫星渲染，全部零运行时资产、TypeScript strict、WebGL2/WebGPU 双栈。**

按技术含量排序的 5 个项目（讲项目经历时按这个顺序，先难后易）：

| 项目 | 一句话 | 你的核心贡献 |
|---|---|---|
| `12_ddgi` | 自研 DDGI 探针 GI，three r185 + WebGPU + TSL | 纯函数核心（octahedral/Fibonacci/Chebyshev）+ trace/blend/border/material-query 四个 kernel |
| `6_patapon3D` | 把暴力体素光线追踪改成 ReSTIR 时空重采样 | Reservoir 无偏纯函数 + 时间重采样 + 一次 NaN/Inf 消毒修复 |
| `11_blackhole` | Kerr 旋转黑洞光子测地线渲染 | Carter 分离的测地线积分器（RK4 + DOPRI5(4) 自适应）+ banding 消除 |
| `14_neuraltexture` | 神经材质：潜变量纹理 + MLP 近似 BRDF | 标量 WGSL MLP + CPU Adam 蒸馏 GGX 老师网络 |
| `13_spysatellite` | EYE-13 夜间 SAR 卫星游戏 | C.A.T 架构下的「轨道谎言」设计（渲染层分屏承担 picking） |

**贯穿全部项目的三条纪律，面试官一定会问，提前备好：**

1. **C.A.T 分层**（Code reuse / Adapter / Token-friendly）：`core/` 是平台无关纯函数，禁止 import three/react/zustand/DOM；`engine/` 是 Web 平台适配器。这样核心数学可单元测试、可被 AI 安全编辑。
2. **CPU/GPU 数值一致**：同一段数学在 `core/*.ts` 和 WGSL/GLSL 里各写一遍，必须逐字节一致；改一边必须改另一边（黑洞 `11_blackhole/src/core/physics/kerr.ts` ↔ `engine/shaders/blackhole.ts`，DDGI `core/fibonacci.ts` ↔ WGSL kernel，神经纹理 `core/mlp.ts` ↔ `engine/NeuralMaterial.ts`）。
3. **冻结参数表 + 验证门槛**：所有数值先写进 `constants.ts` / TDD 冻结，验证是 typecheck + 构建 + 黄金向量单测 + 浏览器 smoke（零 console 错误）。

---

## 1. 跨项目必问高频题

### Q1.1 讲讲你最有技术含量的一个改动

**答（STAR 结构，选 ReSTIR，因为它同时踩了算法、架构、性能、GPU 调试四个考点）：**

- **S（现状）**：`6_patapon3D` 的 `VoxelRaycaster.ts` 是单 pass 全屏片段着色器，每像素 1 主光线 + 5 阴影 + 3 GI + 1 水面反射 ≈ 10 条体素 DDA 光线，确定性采样、零跨帧复用（`RESTIR.md:58-72`）。
- **T（目标）**：用 NVIDIA RTXDI 的 ReSTIR 思路，把「每帧重算全部光照」改成「每像素每帧只生成 1 条候选 + Reservoir 状态 + 时间重采样」。
- **A（行动）**：先写纯函数 `src/core/reservoir.ts`（`combineCandidate` / `combineReservoirs` / `finalizeReservoir` / `packNormal`），再做 3 附件 MRT ping-pong 的时间重采样管线，最后加运动矢量（`setCamera` 差值 + core 快照）。
- **R（结果）**：每像素光线数从 ~10 降到 ~3；但发现了一个反直觉的真相——见 Q2.2，这个坑让答案更有记忆点。

### Q1.2 C.A.T 架构是什么，为什么这么分？

**答：core 是「物理/数学真相」，engine 是「平台谎言」。**

- `core/` 只做纯函数和纯数据，零副作用，零平台依赖。它不知道 orbit、FOV、云层、DOM、GPU。DDGI 的 `core/octahedral.ts`、`core/chebyshev.ts`，黑洞的 `core/physics/kerr.ts`，神经纹理的 `core/mlp.ts`，都是这种。
- `engine/` 把 core 绑到 Web 平台：WebGPU/WebGL2、three.js、React、Web Audio。

**为什么这么分（三个实在理由）：**
1. **可测**：core 是纯函数，可以跑黄金向量单测（`12_ddgi/src/core/chebyshev.test.ts`、`fibonacci.test.ts`）。GPU kernel 没法直接单测，所以把数学抽出来在 CPU 测。
2. **可复用**：同一套 Reservoir 数学，CPU 调试路径和 GPU 着色器共用。
3. **token-friendly**：AI 代理改游戏规则时只碰 `core/` 的数据和纯函数，不碰渲染管线。这也是 SAR 卫星的核心卖点——见 Q2.14。

**边界**：这分层不是「为了整洁」，是为了把「难测的」和「能测的」切开。如果项目没有 AI 编辑需求、没有黄金向量验证，这套分层是过度设计。

### Q1.3 CPU 和 GPU 的数值怎么保证一致？

**答：同一公式写两遍 + 冻结标量 + 黄金向量。**

- 黑洞：`core/physics/kerr.ts`（CPU RK4 + DOPRI5(4) 参考）和 `engine/shaders/blackhole.ts`（GLSL）必须同步改，数值一致（`11_blackhole/AGENTS.md` 双实现一致性规则）。
- DDGI：每帧射线方向集由 `core/fibonacci.ts` 在 CPU 生成上传，GPU kernel 只消费不生成（`12_ddgi/AGENTS.md`）。
- 神经纹理：`core/mlp.ts` 的 forward 和 `engine/NeuralMaterial.ts` 的 WGSL 用同一公式、同一打包布局（`14_neuraltexture/AGENTS.md`）。

**关键技巧**：把冻结常量直接内联成 WGSL 字面量，而不是通过 uniform 传——DDGI 的 `blendKernels.ts:31-36` 就是这么干的（`const hysteresis = PROBE_HYSTERESIS` → 字符串拼接进 shader），从源头关掉 CPU/GPU 漂移。

### Q1.4 WebGL2 和 WebGPU 你怎么选？

**答：先看「要不要通用 compute / atomics」，再看浏览器覆盖面。**

- **DDGI 必须 WebGPU**：probe 累积需要 compute shader 的通用原子操作和 storage buffer。WebGL2 没有 compute，只能用 additive-blend render target 模拟，慢 ~20 倍（`references/ddgi/implementation-plan.md:26`）。
- **ReSTIR 留在 WebGL2**：`6_patapon3D` 冻结栈是 three 0.170 / WebGL2，ReSTIR 的 Reservoir 状态可以用纹理 ping-pong 存，不需要 compute，所以走「WebGL2 Reservoir-lite」路线而不是升级 three 版本（`RESTIR.md:15`）。
- **回退阶梯**：DDGI 的 `navigator.gpu` 门 → baked `LightProbeGrid` → raster；GI 永远是 cosmetic，不是 gameplay 依赖（仓库铁律）。

### Q1.5 零运行时资产怎么理解？

**答：没有一张贴图、一个模型、一个字体文件，所有几何和纹理都是程序化生成，音效是 Web Audio 合成。**

这意味着「美术」也是代码：黑洞的星空/星云/吸积盘是 shader 程序化生成，Patapon 的角色是体素渲染，SAR 卫星的云层和庭院是程序化几何。好处是仓库极小、可复现；代价是「美术表达」受限于你能写出什么 shader。

### Q1.6 你怎么验证渲染正确性？

**答：四道门，从便宜到贵。**

1. **黄金向量单测**：octahedral 编解码、Fibonacci、Chebyshev、Reservoir 打包，用「手算好的向量」做参考测试（`12_ddgi/src/core/*.test.ts`，`6_patapon3D/checks/check-reservoir.ts` 31 PASS）。
2. **解析解对照**：黑洞的 Kerr 影子轮廓有解析公式（`kerrShadowOutline`），用「影子半径 B_CRIT ≈ 2.598」做验证锚点（`11_blackhole/AGENTS.md`）。
3. **RenderDoc 抓帧**：GPU 单步调试、看中间 buffer（这次 NaN/Inf 修复就是 RenderDoc 实锤的，见 Q3.1）。
4. **浏览器 smoke**：Playwright 跑 dev server，断言 `window.__ddgi` / `__sim` / `__neural` 等 DEV 钩子可调用、控制台零 GPU 错误。

---

## 2. 分项目深挖

## 2.1 DDGI（12_ddgi）—— 面试官最可能深挖的项目

**一句话：自研 DDGI 动态漫反射全局光照探针系统，three r185 WebGPU + TSL，每帧在 GPU 上对每个探针做 256 条射线求辐照度 + 深度二阶矩，材质节点用三线性插值 + Chebyshev 可见性拒绝查询。**

### 你实际做了什么（锚点）

- **纯函数核心**（M1）：`src/core/` 下 `octahedral.ts`（八面体编解码）、`fibonacci.ts`（球面 Fibonacci 采样 + Arvo 旋转）、`probeGrid.ts`（三线性权重 + wrap shading）、`chebyshev.ts`、`hysteresis.ts`（EMA）、`moments.ts`。全部有黄金向量单测。
- **trace kernel**：每探针 256 条射线，通过 `three-mesh-bvh/webgpu` 的 `getShapecastFn()` 做 BVH 求交，写 ray-data buffer。
- **blend kernel**（M2）：见 Q2.1.3，这是最关键的实现决策。
- **material query node**（M3）：`DdgiMaterialNode.ts` 的 `ddgiQuery` WGSL 函数，三线性插值 8 个探针 + wrap shading + Chebyshev 拒绝 + 八面体双线性采样。

### Q2.1.1 DDGI 和 SSR / SSAO / 光照贴图有什么区别？

**答：DDGI 是「世界空间、离屏、无限反弹」的漫反射 GI。**

- **SSR**（屏幕空间反射）：只能反射屏幕里看得到的东西，物体走到屏外就消失，而且是镜面反射不是漫反射。
- **SSAO**：只做局部遮蔽变暗，不是光照，不传递颜色。
- **光照贴图**：静态烘焙，光动了就废。
- **DDGI**：把辐照度存在世界空间的一堆「探针」里，探针之间互相采样就自动获得无限反弹；光源移动、物体移动，GI 几帧内跟着更新。它的「定义性正确性检查」是**厚墙漏光测试**：一个发光卡片照不穿一堵厚墙，墙后必须保持黑——靠 Chebyshev 可见性拒绝（`implementation-plan.md:143`）。

### Q2.1.2 探针系统的渲染流程，每个 pass 干什么？

**答：每帧 5 步（`implementation-plan.md:85-98`）：**

```
1. BvhProvider.updateTransforms()   // 场景动了就更新 BVH
2. traceKernel      → ray-data      // 每探针 256 条射线 → shapecast → 命中/未命中/背向分类
3. blendKernel      → irradiance + distance  // 见 Q2.1.3
4. borderKernel     → 八面体 atlas 填充 1 纹素边界
5. render pass      → DdgiMaterialNode 采样 atlas → 间接漫反射
```

第 2-4 步是 `renderer.compute(node)` 派发的 compute pass，第 5 步是普通材质 pass。**反弹 > 1 是隐式的**：探针下一帧采样其他探针，不需要额外 pass（`implementation-plan.md:97`）。

### Q2.1.3 blend pass 的归约怎么做的？为什么没按 RTXGI 原版来？

**答（这是核心考点）：一个线程负责一个 texel，串行遍历该探针的 256 条射线累加，绕开了 RTXGI 的 groupshared 归约。**

- RTXGI 原版：一个线程一条射线，probe 内用 **groupshared 内存**归约所有射线（`implementation-plan.md:156`）。
- three r185 的 TSL 只暴露 `workgroupBarrier()`，**没有 groupshared 数组 helper**，忠实移植等于重写热路径归约。
- 我的落地（`12_ddgi/src/engine/kernels/blendKernels.ts:57-87`）：`gid = workgroupSize.x * workgroupId.x + localId.x` 直接映射到 texel，`probeIdx = gid / (irrI*irrI)`，然后 `for (var r = 0u; r < numRays; r++)` 串行读 rayData 累加 `Σ(L·cosθ) ÷ (2·Σcosθ)`。

**为什么这么改是对的**：DDGI 的探针规模小（每探针 256 射线），串行遍历的代价可控，换来了「不用 hack groupshared」。这是一个「算法级重写 vs 小注释」的权衡——**正确答案是承认这不是脚注级改动，是热路径的结构性改写**。

### Q2.1.4 Chebyshev 可见性拒绝是什么？为什么能防漏光？

**答：探针存的不只是平均距离，还有距离的二阶矩（mean, meanSq），用它估计「到最近表面的距离分布」，做方差加权的可见性拒绝。**

- 一个 texel 存 `mean`（距离均值）和 `meanSq`（距离平方均值），方差 = `mean² - meanSq`（`DdgiMaterialNode.ts:92`）。
- 查询时，如果着色点到探针的距离 `distToProbe` 明显大于 `mean`，说明中间可能隔着墙（光线穿过墙了），用 Chebyshev 不等式算一个权重 `variance / (variance + dv²)` 再立方，把穿墙贡献压掉（`DdgiMaterialNode.ts:95-104`）。
- 这就是「厚墙漏光测试」能通过的数学原因：二阶矩捕捉到了「前方有近表面」的分布信息，一阶矩（只有平均距离）做不到。

### Q2.1.5 `getShapecastFn()` 返回的是什么？耦合有多紧？

**答：返回 WGSL 源码字符串，需要组合进 TSL 的 `Fn`，不是「一个文件就能适配的适配器」——换 BVH 方案等于重写着色器。**

- `three-mesh-bvh/webgpu` 的 `BVHComputeData.getShapecastFn()` 生成 WGSL 的 TLAS+BLAS 遍历源码（`implementation-plan.md:18,155`）。
- 这不是「换个实现类就行」的干净接口，耦合点是 **compute kernel 的 plumbing 本身**。所以我把 BVH 隔离在 `BvhProvider` 后面，但心理预期是「换方案 = shader 重写」，不是「改一行 import」。

### Q2.1.6 主动交代的局限（诚实，防追问）

1. **间接反弹未做**：trace 只取自发光（emissive），探针采样探针的「无限反弹」那条链还没实现（`12_ddgi/AGENTS.md` 已知限制）。所以现在不是真正的「无限 bounce」，是「一次 bounce + EMA 累积」。
2. **border 是 same-edge clamp**：正确的 DDGI 要求八面体 atlas 边界做 opposite-edge wrap，现在是 same-edge clamp（`12_ddgi/AGENTS.md` 已知限制，`borderKernel.ts` 有 NOTE）。
3. **WebGL2 回退未实现**：baked `LightProbeGrid` 路径还只是计划。
4. **material 集成是双记账**：`MeshStandardMaterial` 没有「间接漫反射 GI」注入点，需要自定义材质，还要让 `three-mesh-bvh` 的场景和可见场景保持同步（`implementation-plan.md:157`）——动态物体是两份账。

---

## 2.2 ReSTIR（6_patapon3D）—— 算法 + 性能 + GPU 调试三合一考点

**一句话：把每像素 ~10 条暴力体素光线的确定性渲染，改成「1 条随机候选 + Reservoir 加权水库采样 + 时间重采样」，有效采样数从每帧 1 累积到数百，光线数反而下降。**

### 你实际做了什么（锚点）

- **纯函数核心**：`src/core/reservoir.ts` —— `combineCandidate`（`:97`，流式加权接受/拒绝）、`combineReservoirs`（`:126`）、`finalizeReservoir`（`:139`，W/(M·p̂) 归一化）、`packNormal`（`:184`，八面体 + 2×16-bit snorm 压成 1 uint32）、`packAgeM`（`:202`）。
- **时间重采样**：`VoxelRaycaster.ts` 拆成 3 附件 MRT ping-pong，GI 改 1 随机余弦次级光线 + Reservoir 时间复用，阴影改 1-tap + EMA。
- **运动矢量**：`SceneContract.CameraState` 加 `motionVector`（屏幕空间 xy=像素位移，z=线性深度差），静态体素零运动矢量。

### Q2.2.1 ReSTIR 的核心思想是什么？为什么能「光线少但质量高」？

**答：ReSTIR 用 Reservoir（水库）跨帧、跨像素复用采样结果，把「有效采样数」和「每帧实际光线数」解耦。**

- **Reservoir 数据结构**：`(WeightSum, M, 选中样本, Age)`。`M` 记录「看到过的候选总数」——时间+空间合并后 `M` 逐帧累积，每像素有效采样数 = M（可达数百），但每帧实际光线数还是 ~1（`RESTIR.md:86`）。
- **无偏性的心脏是加权水库采样**（`reservoir.ts:97-119`）：`risWeight = targetPdf × sampleNormalization`，`M += sampleM`，`WeightSum += risWeight`，然后 `random × WeightSum < risWeight` 就替换选中样本。这不是「平均」，而是按权重概率接受——**重用的样本只要带正确权重，就不是作弊，是无偏估计**。
- 最终 `finalizeReservoir` 用 `W / (M · p̂)` 归一化（`reservoir.ts:139-145`），得到无偏积分。

### Q2.2.2 为什么 M2 落地后，在 SwiftShader 上反而更慢了 129%？

**答（这是最有记忆点的题，一定要答出「架构真相」）：ReSTIR 优化的是「光线数」，不是「带宽」；SwiftShader 恰好是带宽瓶颈，真 GPU 才是光线数瓶颈。**

数据（`RESTIR.md:241-248`）：

| 指标 | M1 基线 | M2 实测 | 结论 |
|---|---|---|---|
| intro 1280×720 | 177ms | **405ms** | +129% |
| intro 390×844 | 62ms | **183ms** | 回归 |

- **ReSTIR 的收益**：每像素 ~10 条 DDA → ~3 条，这在真 GPU 上是实打实的着色器算力节省。
- **但 M2 的代价**：把「单 pass 单附件 RGBA8 直出」换成「3×RGBA16F MRT（24B/px 写）+ blit 读回 + 3 次历史纹理采样」——**带宽成本吞掉了光线数收益**。
- **SwiftShader 是软件光栅化**，带宽主导；**真 GPU 才是光线数主导**。所以「优化方向」和「测量环境」错配了。

**引申考点**：这题考的是「你是否理解优化指标的物理本质」——不是所有性能优化在软渲染器上都成立。正确的验收应该换真 GPU 帧时。

### Q2.2.3 时间重采样怎么防「幽灵」和错误累积？

**答：三件套——运动矢量重投影 + 表面校验 + Age 上限。**

- **重投影**：`prevUv = (gl_FragCoord.xy + motionVector.xy) / res`，把当前像素映射回上一帧位置。
- **表面校验**：读上一帧的 `prevSurface`（法线 + 深度 + M），法线相似 + 深度接近才复用；disocclusion（运动矢量无效区）用零矢量 fallback（`RESTIR.md:176-181`）。
- **Age 上限**：`packAgeM` 里 age 是 8-bit 夹紧，超过 `maxReservoirAge` 弃用（对照 RTXDI `RTXDI_PTRESERVOIR_AGE_MAX=31`）。

### Q2.2.4 主动交代的局限

1. **只做了时间重采样，没做空间重采样**：完整 ReSTIR 的「读 ≤32 邻居 pairwise MIS 合并」是 M3，还没做。所以相机大幅移动时噪声下降有限。
2. **跳过 PT 路径重连**：Hybrid Shift / Jacobian / 路径重放是「完整 ReSTIR PT」才需要的，WebGL2 下单文件 fragment shader 下合理裁剪（`RESTIR.md:186`）。
3. **阴影确定性契约改动**：从「禁逐像素抖动」改成「双模采样」（确定性基线 / ReSTIR 随机初始采样 + 时间收敛去噪），这是契约变更，走了 TDD changelog。

---

## 2.3 Kerr 黑洞（11_blackhole）—— 数值方法 + 物理正确性考点

**一句话：Kerr 旋转黑洞的光子测地线渲染——Carter 分离把广义相对论测地线方程降维成一阶 ODE，GPU 用 RK4 固定步积分，CPU 用 DOPRI5(4) 自适应做高精度验证。**

### 你实际做了什么（锚点）

- **测地线积分**：`core/physics/kerr.ts` 的 `kerrTracePhoton`（RK4 固定步，`:310`）和 `kerrTracePhotonAdaptive`（DOPRI5(4) + PI 步长控制，`:529`）。
- **数值稳定性关键决策**（`kerr.ts:9-16` 注释里写得明明白白）：
  - Δ(r) 因式分解成 `(r−r₊)(r−r₋)`，避免 `r²−2Mr+a²` 在地平线附近的灾难性消去（float32 安全）。
  - 极坐标用 `u = cosθ` 积分而不是 θ，极点处正则，角势能变成干净的二次式。
  - 转折点用精确二分而不是符号钳位。
  - 守恒量（E, L_z, Q）在 seed 处固定，Carter 形式硬编码它们，永不漂移。
- **盘物理**：Bardeen-Press-Teukolsky ISCO（`kerrISCO`）、Keplerian 角速度、多普勒 + 引力红移 `g = 1/[u^t(1−Ωλ)]`（`kerrRedshift`）、Shakura-Sunyaev 盘 `r⁻³` emissivity / `r⁻³ᐟ⁴` temperature。
- **banding 消除**（最近 commit）：Kerr emissivity + bloom 阈值、三角 dither + 真设备分辨率渲染、蓝噪 dither。

### Q2.3.1 测地线方程怎么解？为什么用 Carter 分离？

**答：完整测地线是二阶耦合 ODE，Carter 分离把 Kerr 时空的测地线方程降维成「径向 + 极向 + 方位角」三个一阶方程，每个方向有独立的势能函数。**

- Kerr 时空有 Carter 常数 Q（第四运动常数，除了 E、L_z、静质量之外），让 r 和 θ 方向可分离。
- 径向势能 `R(r) = P² − Δ(η + (λ−a)²)`，极向势能 `Θ(u) = η + (a²−η−λ²)u² − a²u⁴`（`kerr.ts:198-199`）。
- 光子只能存在于 `R ≥ 0` 且 `Θ ≥ 0` 的区域，转折点是 `R=0` / `Θ=0` 的根——用二分精确求（`bisectRadial` / `bisectPolar`），而不是「步进撞到负值就钳到 0」的粗暴法。

### Q2.3.2 为什么 GPU 用 RK4、CPU 用 DOPRI5(4)？

**答：GPU 要确定性、固定步长、着色器可写；CPU 要自适应精度做验证。**

- GPU（`blackhole.ts` shader）：RK4 固定步，步长 `dL = clamp(0.16·r, 0.03, 1.5)`（`kerr.ts:342`），简单、无分支发散。
- CPU 参考（`kerrTracePhotonAdaptive`）：DOPRI5(4) 是 7 阶段 Dormand-Prince 对（5 阶解 + 4 阶误差估计），PI 步长控制器。**关键技巧是换到 Mino time（`dτ = Σdλ`）**，去掉刚性 `1/Σ` 前缀，让 √R 和 √Θ 在转折点平滑消失，高阶积分器才良态（`kerr.ts:402-408`）。
- 二者数值一致，CPU 自适应版用来验证 GPU 固定步版没跑偏。

### Q2.3.3 光子环和黑洞影子怎么验证对不对？

**答：用解析解对照。**

- Schwarzschild 极限下影子是完美圆，半径 `b_crit = 3√3 M`（`kerr.ts:673-681` 的 `kerrShadowOutline` 特殊分支）。
- Kerr 有自旋时影子是 D 形，用临界曲线解析参数化（`kerrShadowOutline` 的 `rpMin`/`rpMax` 端点 + λ、η 公式）。
- 验证锚点（`11_blackhole/AGENTS.md`）：影子半径 `B_CRIT ≈ 2.598`（bhu 单位下 3√3·M，M=1/2），ISCO 轨道速度 `0.5c`，光子球 `1.5`。

### Q2.3.4 8-bit banding 怎么消的？

**答：三条路依次上——Kerr emissivity 重标定 + bloom 阈值、三角 dither、蓝噪 dither。**

- 8-bit 显示下，深空渐变区域量化阶梯（banding）明显。
- 先调 Kerr 发射率 + bloom 阈值，把亮度分布拉开（`fix(blackhole): Kerr emissivity + bloom threshold`）。
- 再上**三角 dither**（三角形概率分布的抖动，比均匀 dither 更均匀）+ **真设备分辨率渲染**（不是固定低分辨率再放大），杀掉残余 banding（`fix(blackhole): triangular dither + true device-res render`）。
- 最后**蓝噪 dither**（`e01c1c2 fix(blackhole): add blue-noise dither pass`），蓝噪频谱集中在高频，人眼对高频噪声最不敏感，视觉上比白噪「干净」。

### Q2.3.5 主动交代的局限

- **软阴影 / 盘的自阴影、盘的热谱**是近似（Shakura-Sunyaev 解析剖面），不是完整辐射传输。
- 光子环的多次绕转图像（n≥1 环）在 GPU 固定步版里可能因步数限制被截断（CPU 自适应版才完整打标 `index`）。

---

## 2.4 神经纹理（14_neuraltexture）—— 机器学习 + 图形交叉考点

**一句话：用一张 8 维潜变量纹理 + 一个小 MLP（14→32→32→3）近似 `BRDF(uv, ωi, ωo)`，浏览器里跑标量 WGSL MLP，CPU 用 Adam 蒸馏 GGX 老师网络烘焙权重。**

### 你实际做了什么（锚点）

- **网络结构**（`core/constants.ts`）：`LATENT_DIM=8`，输入 `DECODER_IN = 8 + 6 = 14`（8 潜变量 + wh.xyz + wd.xyz），隐藏层 `HIDDEN_WIDTH=32`，输出 RGB 3，总参数量 `MLP_PARAM_COUNT = 1635`（`constants.ts:29`）。课程原版 4096²×8 潜变量纹理占 512 MiB，我们只 ship 64²×8（`constants.ts:5`）。
- **前向/反向**：`core/mlp.ts` 的 `decode`（`:57`）、`forward`（`:82`）、`backwardLogL1`（`:112`）。
- **训练**：`scripts/bake.ts` CPU Adam 蒸馏 GGX 老师 → 写 `src/engine/baked.ts`。
- **GPU**：`engine/NeuralMaterial.ts` 标量 WGSL，和 `core/mlp.ts` 逐公式一致。

### Q2.4.1 潜变量 + MLP 是什么结构？为什么不是「大模型直接生成贴图」？

**答：网络预测的是 BRDF 值 `material_eval(uv, wi, wo) -> RGB`，不是最终像素颜色。**

- BRDF 是材质属性（光从 wi 来，散射到 wo 有多少能量），不是乘了灯光曝光的像素颜色。
- 结构拆两半：**UV 从潜变量纹理取一个短的局部代码（8 维），所有纹素共享的 Decoder MLP 把「局部代码 + 方向信息」解码成 RGB 响应**（拆解文 §图1）。
- 这不是「自动获得压缩」——潜变量通道数、精度、纹理分辨率、Decoder 大小共同决定内存。课程只证明了「能表示」，没给产品级压缩率。

### Q2.4.2 为什么用 Rusinkiewicz 参数化？它解决了什么？

**答：把 BRDF 已知的方向结构显式编码进网络输入，让有限容量的小 MLP 不用从 6 个原始方向分量里重新推导。**

- 直接喂 `wi`、`wo` 的 6 个分量，小 MLP 学不好高光——高光波瓣主要随**半程向量 wh = normalize(wi+wo)** 变化，Fresnel 跟观察方向和半程向量的夹角有关。
- Rusinkiewicz 参数化用 `(wh, wd)`（wd 是把 wi 旋转到 wh 坐标系后的差分向量）替代原始方向（拆解文 §Rusinkiewicz）。
- **核心观点（面试官爱听）**：神经网络没有取消材质理论，反而更依赖它——把 BRDF 里已知的对称性编码进输入，省下的参数留给难手工建模的部分。

### Q2.4.3 loss 怎么设计？为什么不用普通 L1？

**答：log-L1，先对输出做 log 压缩，防止高光的巨大动态范围支配梯度。**

- BSDF 输出可能远大于 1，镜面材质把能量集中到很窄方向，峰值很高。
- 直接 L1 会让少数极高样本支配训练。
- 我用的 loss（`mlp.ts:112-125`）：`mean over RGB of |log1p(y) - log1p(target)|`，输出层用 exp 激活（`mlp.ts:64-68`），反向梯度是 `sign · y/(1+y)`（`mlp.ts:122-123` 注释里有完整推导）。

### Q2.4.4 为什么浏览器里跑标量 MLP，不用 Cooperative Vector？

**答：Cooperative Vector 需要 Windows/Linux + NVIDIA GPU + Vulkan `VK_NV_cooperative_vector`，浏览器（WebGPU/WGSL）没有这条路，所以退化成标量 WGSL 逐矩阵向量乘。**

- 课程演示里 Cooperative Vector 在 RTX 3090/5090 上比非 CV 实现快 3 倍以上，但那是**神经纹理训练**（100MB PNG 输入），不是端到端渲染，而且 macOS 路径不支持（拆解文 §图7 + 边界）。
- 我们的 MLP 只有 1635 个参数，标量 matVec 完全跑得动。**「小网络 + 标量」是浏览器落地的最优解，不值得为 CV 牺牲可移植性。**

### Q2.4.5 主动交代的局限

1. **没有 importance sampling / PDF**：神经材质只学了 eval，没学 sample()/pdf()，高光材质用余弦半球采样不是高效重要性采样（拆解文 §边界）。
2. **没有能量守恒硬约束**：网络输出不保证互易性、能量守恒，训练集覆盖不到的方向可能不符合物理。
3. **没有 learned shading frame / Material Encoder 网络**：course step 06/07 没做，用固定打包 encoder（`14_neuraltexture/AGENTS.md` 已知限制）。
4. **这是「近似 BRDF」，不是「神经纹理压缩 NTC」**：NTC 是压缩 PBR 贴图（另一篇论文），别混。

---

## 2.5 SAR 卫星（13_spysatellite）—— 架构设计 + 渲染层分屏考点

**一句话：EYE-13 夜间 SAR 卫星游戏，核心是「轨道谎言」——你在 412 公里高空，但渲染层分成两层：OrbitShell 只负责天空/边缘/云层（不可点），CourtyardTile 20×20m 庭院承担全部 picking 和 SAR 热斑。**

### 你实际做了什么（锚点）

- **C.A.T 三件套**：`core/` 是 20m 庭院模拟（heat/lock/radio），`engine/` 是 OrbitShell + CourtyardTile + CameraRig + Input + Audio，`components/` 是 React HUD。
- **核心抽象 `zoom01`**：一个 0..1 输入，只门控「我们是否在 tile 上」，core 永远不知道 orbit/FOV/云层（`13_spysatellite/AGENTS.md` §4）。
- **picking 分流**：光学剪影不可点，SAR 热斑可点；SAR 开启时才能 lock VIP。

### Q2.5.1 「orbit lie」是什么意思？为什么这么设计？

**答：这是一个设计决策，不是技术难点——把「你是一颗卫星」的幻觉，拆成渲染层和模拟层的两个谎言。**

- **Core 永远不知道自己在地球轨道上**：它只看到 `zoom01 ∈ [0,1]` 这个抽象输入，`COURTYARD_ZOOM = 0.72` 门控 picking 和 lock（`TDD.md:17`）。
- **Engine 拥有轨道谎言**：OrbitShell 画天空/地球边缘/云层，CameraRig 只做 zoom lerp，CourtyardTile 画 20×20m 庭院 + SAR 热斑 + 承担全部 picking。
- **为什么**：把「轨道视角怎么缩放」和「游戏规则（heat/lock/radio）」彻底解耦。规则层是纯函数可测，视角层是纯表现。这也是 C.A.T 里 token-friendly 的直接体现——AI 改游戏规则时完全不碰渲染。

### Q2.5.2 SAR 热度机制和 lock 丢失规则？

**答（数值，可能被追问）：**

- SAR heat：`+18%/s` 开启，`−12%/s` 关闭，满 `100%` → `2.5s` 强制 drop + 丢失 lock（`TDD.md` 数值表）。
- Lock：SAR 开启时点 VIP。丢失条件（`13_spysatellite/AGENTS.md` §5）：
  - 树冠遮挡 `≥0.4s`
  - SAR 关闭 `≥0.6s`
  - 过热
  - zoom 越过 `COURTYARD_ZOOM 0.72`（缩回轨道）
- 击杀时刻 80s 要求 lock 持续最后 `2s` + beat 7 GO 按时发出。

### Q2.5.3 主动交代的局限

- **7 个 radio beats 是脚本化的**，不是动态生成的（`GDD.md` §7 scope thirds，LLM paraphrase 是 M2+）。
- 没有 planet mesh、没有 orbit picking、没有「你扣扳机」（JTAC 是 M2+）——这些是明确的 forbidden（`13_spysatellite/AGENTS.md` §6）。

---

## 3. GPU 调试方法论（RenderDoc + NaN/Inf 案例，渲染岗必考）

**这是跨项目的通用能力，面试官很可能单独问「你用过 RenderDoc 吗？怎么定位一个 GPU bug？」**

### Q3.1 讲一次你用 RenderDoc 定位 NaN/Inf 的实际案例

**答（`6bf90da` 那次修复，有完整证据链）：**

**现象**：体素光线追踪渲染里出现垃圾负 t，雾计算溢出 -Inf，画面出现黑块/异常。

**定位（RenderDoc 抓帧）**：抓到 `hit.t = -3.1e29`，追到 `marchFine` 的 DDA 步进——**退化轴**：当光线方向 `rd` 的某个分量恰为 0 时，`sign=0 → step=0`，该轴 `tMax = (边界-ro) · ±1e30`，负值会被 `min` 步进选中，产生垃圾负 t（`VoxelRaycaster.ts` F0a 注释）。

**修复**：强制退化轴 `tMax = 1e30` 永不参与步进选择（`marchFine` 和 `marchGrid` 两处都改）。

**但这不是全部**，还有一类更难缠的：**历史纹理的 NaN 跨帧自锁**。所以我用了一个统一的「clamp 即消毒」策略：

- **写侧钳制**：辐射度 `clamp(candRadiance, 0, 10)`（上限对照 RTXDI `c_MaxIndirectRadiance=10`）；`finalW = clamp(weightSum/denom, 0, 6e4)`（RGBA16F 上限 65504，溢出 +Inf 会永久自锁）。
- **读侧钳制**：读历史纹理时 `clamp(prevGi, vec4(0), vec4(10,10,10,6e4))`、`clamp(prevSurf, ...)`。

### Q3.2 为什么用 `clamp` 消毒，而不是 `isnan/isinf` 显式判断？

**答（这是深入考点，很多人不知道）：FXC fast-math 会把 `isnan`/`isinf` 优化成空操作（X3577 已知问题），而 `min/max` 在 D3D 下对 NaN 有确定的 IEEE minNum 语义——返回另一个操作数。**

- `min(NaN, x)` / `max(NaN, x)` 在 IEEE 754-2008 minNum 语义下会「选非 NaN 的那一个」，所以 `clamp(v, lo, hi)` 天然把 NaN 钳回界内：`-Inf→lo`、`+Inf→hi`、`NaN→界内`。
- 这是「借 GPU 的 min/max 语义做消毒」，不依赖 `isnan`（后者在 ANGLE→D3D 的 fast-math 路径下会被编译器优化掉）。

### Q3.3 RenderDoc 定位流程（标准话术）

1. **抓帧**：复现 bug 的帧，看 draw call 列表，定位到出问题的 pass。
2. **看中间 buffer**：DDGI 用 `window.__ddgi.readProbeSummary()` 读回 rayData buffer；ReSTIR 看 reservoir 纹理。
3. **Pixel History / Mesh Viewer**：单像素追踪「谁写了这个错误值」。
4. **Shader Debugger**：单步看变量，实锤 `hit.t=-3.1e29` 这种中间值。
5. **对照 CPU 参考**：黄金向量单测 + CPU 参考实现（黑洞 DOPRI5 版、神经纹理 `core/mlp.ts`）交叉验证。

---

## 4. 进场前 10 分钟速记表

### 关键数字（面试可能被问「具体值」）

| 项目 | 数字 | 出处 |
|---|---|---|
| ReSTIR 光线数 | ~10 → ~3 条/像素 | `RESTIR.md:194` |
| ReSTIR M2 回归 | 177ms→405ms（+129%） | `RESTIR.md:245` |
| ReSTIR 采样累积 | M 可达数百，帧光线 ~1 | `RESTIR.md:86` |
| DDGI 探针射线 | 256 条/探针 | `implementation-plan.md:104` |
| DDGI 八面体分辨率 | 辐照度 6（8 带边），距离 16（18 带边） | `implementation-plan.md:106-107` |
| DDGI hysteresis | α=0.97，threshold drop −0.75 | `implementation-plan.md:108` |
| 神经纹理 MLP | 8 latent + 6 方向 = 14 输入 → 32 → 32 → 3，共 1635 参数 | `constants.ts:11,29` |
| 神经纹理潜变量 | 64²×8（课程 4096²×8 = 512 MiB） | `constants.ts:5,9` |
| 黑洞影子半径 | b_crit = 3√3 M ≈ 2.598 bhu | `11_blackhole/AGENTS.md` |
| Kerr ISCO | 自旋越高越近（prograde） | `kerr.ts:40-48` |
| SAR heat | +18%/s 开，−12%/s 关，100%→2.5s drop | `TDD.md:41-43` |
| SAR lock 丢失 | 树冠≥0.4s / SAR 关≥0.6s / zoom>0.72 | `13_spysatellite/AGENTS.md` |

### 一句话防问倒（每个项目一个「最怕被问」的点，提前背）

- **DDGI**：blend 归约为什么是「每 texel 线程串行遍历 256 射线」而不是 RTXGI 的 groupshared——因为 TSL 没有 groupshared 数组 helper（`blendKernels.ts:57-87`）。
- **ReSTIR**：为什么软渲染器上更慢——优化光线数 vs 带宽，SwiftShader 带宽主导（`RESTIR.md:248`）。
- **黑洞**：为什么 GPU RK4 / CPU DOPRI5(4)——确定性 vs 自适应精度验证；Mino time 去刚性。
- **神经纹理**：为什么不 CV——浏览器没有 Cooperative Vector，标量 1635 参数跑得动。
- **SAR**：core 不知道自己在轨道上——`zoom01` 抽象门控，orbit lie 在 engine。

### 面试官可能的「压力追问」清单（提前想好诚实边界）

1. 「你的 DDGI 和 RTXGI 原版比，缺了什么？」→ 间接反弹、opposite-edge border、WebGL2 回退、relocate/classify kernel（M3/M4 没做）。
2. 「你的 ReSTIR 是无偏的吗？」→ Reservoir 流式合并是无偏加权水库采样；但阴影 EMA 复用、GI 的确定性余弦瓣 + 时间复用是「有偏但收敛」的工程近似。
3. 「神经材质的误差有多少？」→ 三球场景里右球显示 `|error|×8`，baked 后有 `bakedValL1` 可查（`window.__neural`），但没有和传统 PBR 的端到端性能/显存对照表（课程也没给）。
4. 「为什么不用现成的库？」→ DDGI 没有成熟可依赖的三.js 库（`speedball-gi` v0.6.7 单作者、有泄漏、装依赖有坑），ReSTIR 参考 RTXDI 是 NVIDIA 专有许可只移植算法不抄代码。

---

*完。面经核心：每个项目能讲清「是什么 → 我做了什么（file:line）→ 最难的点 → 主动承认的局限」，比背一堆名词值钱得多。*
